apiVersion: v1
kind: Service
metadata:
  name: speedtest-tracker-service
  namespace: default
spec:
  selector:
    app: speedtest-tracker
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080


