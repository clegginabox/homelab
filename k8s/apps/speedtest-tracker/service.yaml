apiVersion: v1
kind: Service
metadata:
  name: speedtest-tracker-service
  namespace: default
spec:
  selector:
    app: speedtest-tracker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
  loadBalancerIP: 192.168.5.110
